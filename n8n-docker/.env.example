LETSENCRYPT_EMAIL=you@example.com
N8N_VERSION=1.0.0
N8N_HOST=example.com
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=admin
# Do not put plaintext passwords here. Create secret files in ./secrets/
POSTGRES_DB=n8n
POSTGRES_USER=n8n

# Secrets files (create these locally, not checked into git):
# ./secrets/postgres_password
# ./secrets/n8n_basic_auth_password
